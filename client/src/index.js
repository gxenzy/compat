// Adapter file for React apps that expect index.js
// This simply imports the TypeScript entry point file

// Import the TypeScript entry point
import './index.tsx';

// Note: index.tsx doesn't export anything that we can re-export,
// it just renders the React application to the DOM 