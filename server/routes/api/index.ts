/**
 * API Routes Index
 * This file serves as a central hub for all API routes
 */
import express from 'express';

// Create a router for the API
const apiRouter = express.Router();

// Register API routes
// Training routes removed as no longer needed

// Export the router
export default apiRouter; 